# oc-git-activite

Dans ce projet nous avons une classe utilitaire 'Utils' qui contient une méthode static 'findMax' qui prend comme argument une liste de Double.

Cette méthode:

- retourne 'null' lorsqu'il n'y a aucun nombre dans la liste
- retourne le maximum sinon.